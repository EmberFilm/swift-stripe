//
//  SubscriptionSchedulesClient.swift
//  Stripe
//
//  Generated by Scripts/generate-requests.py from Stripe's OpenAPI spec (2026-07-29.dahlia).
//  Do not edit by hand.
//

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
import NIOHTTP1

/// Operations on Stripe.Billing.Subscription.Schedule.
///
/// A protocol so tests can substitute a double; ``SubscriptionSchedulesClient`` is the implementation that
/// talks to Stripe.
public protocol SubscriptionSchedulesAPI: Sendable {
    func cancel(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Cancel.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Cancel.Response
    func create(_ request: Stripe.Billing.Subscription.Schedule.Create.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Create.Response
    func list(_ request: Stripe.Billing.Subscription.Schedule.List.Request) async throws -> Stripe.Billing.Subscription.Schedule.List.Response
    func release(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Release.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Release.Response
    func retrieve(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Retrieve.Request) async throws -> Stripe.Billing.Subscription.Schedule.Retrieve.Response
    func update(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Update.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Update.Response
}

public struct SubscriptionSchedulesClient: SubscriptionSchedulesAPI {
    private let api: StripeAPI

    public init(api: StripeAPI) { self.api = api }

    public func cancel(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Cancel.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Cancel.Response {
        try await api.send(.POST, "v1/subscription_schedules/\(id)/cancel", body: request, idempotencyKey: idempotencyKey)
    }

    public func create(_ request: Stripe.Billing.Subscription.Schedule.Create.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Create.Response {
        try await api.send(.POST, "v1/subscription_schedules", body: request, idempotencyKey: idempotencyKey)
    }

    public func list(_ request: Stripe.Billing.Subscription.Schedule.List.Request) async throws -> Stripe.Billing.Subscription.Schedule.List.Response {
        try await api.list("v1/subscription_schedules", parameters: request)
    }

    public func release(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Release.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Release.Response {
        try await api.send(.POST, "v1/subscription_schedules/\(id)/release", body: request, idempotencyKey: idempotencyKey)
    }

    public func retrieve(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Retrieve.Request) async throws -> Stripe.Billing.Subscription.Schedule.Retrieve.Response {
        try await api.list("v1/subscription_schedules/\(id)", parameters: request)
    }

    public func update(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Update.Request, idempotencyKey: String?) async throws -> Stripe.Billing.Subscription.Schedule.Update.Response {
        try await api.send(.POST, "v1/subscription_schedules/\(id)", body: request, idempotencyKey: idempotencyKey)
    }
}

// A write with no explicit key behaves as it did before idempotency keys existed: no header,
// and no retry. See ``StripeAPI/isSafeToRetry(_:)``.
extension SubscriptionSchedulesAPI {
    public func cancel(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Cancel.Request) async throws -> Stripe.Billing.Subscription.Schedule.Cancel.Response {
        try await cancel(id: id, request, idempotencyKey: nil)
    }

    public func cancel(id: Stripe.Billing.Subscription.Schedule.ID, idempotencyKey: String? = nil) async throws -> Stripe.Billing.Subscription.Schedule.Cancel.Response {
        try await cancel(id: id, .init(), idempotencyKey: idempotencyKey)
    }

    public func create(_ request: Stripe.Billing.Subscription.Schedule.Create.Request) async throws -> Stripe.Billing.Subscription.Schedule.Create.Response {
        try await create(request, idempotencyKey: nil)
    }

    public func create(idempotencyKey: String? = nil) async throws -> Stripe.Billing.Subscription.Schedule.Create.Response {
        try await create(.init(), idempotencyKey: idempotencyKey)
    }

    public func list() async throws -> Stripe.Billing.Subscription.Schedule.List.Response {
        try await list(.init())
    }

    public func release(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Release.Request) async throws -> Stripe.Billing.Subscription.Schedule.Release.Response {
        try await release(id: id, request, idempotencyKey: nil)
    }

    public func release(id: Stripe.Billing.Subscription.Schedule.ID, idempotencyKey: String? = nil) async throws -> Stripe.Billing.Subscription.Schedule.Release.Response {
        try await release(id: id, .init(), idempotencyKey: idempotencyKey)
    }

    public func retrieve(id: Stripe.Billing.Subscription.Schedule.ID) async throws -> Stripe.Billing.Subscription.Schedule.Retrieve.Response {
        try await retrieve(id: id, .init())
    }

    public func update(id: Stripe.Billing.Subscription.Schedule.ID, _ request: Stripe.Billing.Subscription.Schedule.Update.Request) async throws -> Stripe.Billing.Subscription.Schedule.Update.Response {
        try await update(id: id, request, idempotencyKey: nil)
    }

    public func update(id: Stripe.Billing.Subscription.Schedule.ID, idempotencyKey: String? = nil) async throws -> Stripe.Billing.Subscription.Schedule.Update.Response {
        try await update(id: id, .init(), idempotencyKey: idempotencyKey)
    }
}
