//
//  Generated by Scripts/generate-models.py — do not edit.
//  Source: Stripe OpenAPI spec, version 2026-07-29.dahlia
//  Schema: source_mandate_notification
//

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif

/// Source mandate notifications should be created when a notification related to a source mandate must be sent to the pay…
public struct SourceMandateNotification: Codable, Hashable, Sendable, Identifiable {
    public typealias ID = String
    public let id: ID
    /// String representing the object's type.
    public let object: String
    /// A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero…
    public var amount: Int?
    /// Time at which the object was created.
    public var created: Date?
    /// If the object exists in live mode, the value is `true`.
    public var livemode: Bool?
    /// The reason of the mandate notification.
    public var reason: String?
    /// The status of the mandate notification.
    public var status: String?
    /// The type of source this mandate notification is attached to.
    public var `type`: String?
    /// The payload `type` selects.
    public var details: Details

    fileprivate enum CodingKeys: String, CodingKey {
        case id
        case object
        case amount
        case created
        case livemode
        case reason
        case status
        case `type`
        case acssDebit
        case bacsDebit
        case sepaDebit
        case source
    }

    public init(
        id: ID,
        object: String,
        amount: Int? = nil,
        created: Date? = nil,
        livemode: Bool? = nil,
        reason: String? = nil,
        status: String? = nil,
        `type`: String? = nil,
        details: Details
    ) {
        self.id = id
        self.object = object
        self.amount = amount
        self.created = created
        self.livemode = livemode
        self.reason = reason
        self.status = status
        self.`type` = `type`
        self.details = details
    }

    public init(from decoder: any Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(ID.self, forKey: .id)
        self.object = try container.decode(String.self, forKey: .object)
        self.amount = try container.decodeIfPresent(Int.self, forKey: .amount)
        self.created = try container.decodeIfPresent(Date.self, forKey: .created)
        self.livemode = try container.decodeIfPresent(Bool.self, forKey: .livemode)
        self.reason = try container.decodeIfPresent(String.self, forKey: .reason)
        self.status = try container.decodeIfPresent(String.self, forKey: .status)
        self.`type` = try container.decodeIfPresent(String.self, forKey: .`type`)
        self.details = try Details(type: try container.decodeIfPresent(String.self, forKey: .type) ?? "", from: container)
    }

    public func encode(to encoder: any Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(object, forKey: .object)
        try container.encodeIfPresent(amount, forKey: .amount)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(livemode, forKey: .livemode)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(`type`, forKey: .`type`)
        try details.encode(into: &container)
    }

    public struct AcssDebit: Codable, Hashable, Sendable {
        /// The statement descriptor associate with the debit.
        public var statementDescriptor: String?

        public init(
            statementDescriptor: String? = nil
        ) {
            self.statementDescriptor = statementDescriptor
        }
    }

    public struct BacsDebit: Codable, Hashable, Sendable {
        /// Last 4 digits of the account number associated with the debit.
        public var last4: String?

        public init(
            last4: String? = nil
        ) {
            self.last4 = last4
        }
    }

    public struct SepaDebit: Codable, Hashable, Sendable {
        /// SEPA creditor ID.
        public var creditorIdentifier: String?
        /// Last 4 digits of the account number associated with the debit.
        public var last4: String?
        /// Mandate reference associated with the debit.
        public var mandateReference: String?

        public init(
            creditorIdentifier: String? = nil,
            last4: String? = nil,
            mandateReference: String? = nil
        ) {
            self.creditorIdentifier = creditorIdentifier
            self.last4 = last4
            self.mandateReference = mandateReference
        }
    }

    /// The payload `type` selects; `unknown` carries a type this package does not model.
    public indirect enum Details: Hashable, Sendable {
        case acssDebit(AcssDebit)
        case bacsDebit(BacsDebit)
        case sepaDebit(SepaDebit)
        case source(Source)
        case unknown(type: String)

        public var acssDebit: AcssDebit? { if case .acssDebit(let value) = self { return value }; return nil }
        public var bacsDebit: BacsDebit? { if case .bacsDebit(let value) = self { return value }; return nil }
        public var sepaDebit: SepaDebit? { if case .sepaDebit(let value) = self { return value }; return nil }
        public var source: Source? { if case .source(let value) = self { return value }; return nil }

        fileprivate init(type: String, from container: KeyedDecodingContainer<CodingKeys>) throws {
            switch type {
            case "acss_debit":
                if let value = try container.decodeIfPresent(AcssDebit.self, forKey: .acssDebit) { self = .acssDebit(value) } else { self = .unknown(type: type) }
            case "bacs_debit":
                if let value = try container.decodeIfPresent(BacsDebit.self, forKey: .bacsDebit) { self = .bacsDebit(value) } else { self = .unknown(type: type) }
            case "sepa_debit":
                if let value = try container.decodeIfPresent(SepaDebit.self, forKey: .sepaDebit) { self = .sepaDebit(value) } else { self = .unknown(type: type) }
            case "source":
                if let value = try container.decodeIfPresent(Source.self, forKey: .source) { self = .source(value) } else { self = .unknown(type: type) }
            default: self = .unknown(type: type)
            }
        }

        fileprivate func encode(into container: inout KeyedEncodingContainer<CodingKeys>) throws {
            switch self {
            case .acssDebit(let value): try container.encode(value, forKey: .acssDebit)
            case .bacsDebit(let value): try container.encode(value, forKey: .bacsDebit)
            case .sepaDebit(let value): try container.encode(value, forKey: .sepaDebit)
            case .source(let value): try container.encode(value, forKey: .source)
            default: break
            }
        }
    }
}
